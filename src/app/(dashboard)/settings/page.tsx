import { UserRound } from "lucide-react";
import { createClient } from "@/lib/supabase/server";
import { updateProfile } from "./actions";

export default async function SettingsPage() { const supabase = await createClient(); const { data: profile } = await supabase.from("profiles").select("*").single(); return <div className="mx-auto max-w-3xl px-5 py-8 sm:px-8 lg:px-10"><p className="text-sm font-bold text-[var(--brand)]">PERSONAL WORKSPACE</p><h1 className="font-display mt-1 text-4xl font-extrabold tracking-[-.045em]">Pengaturan</h1><p className="mt-2 text-[var(--muted)]">Perbarui informasi yang muncul di workspace pribadimu.</p><form action={updateProfile} className="mt-8 rounded-2xl border border-[var(--line)] bg-white p-6"><div className="grid h-11 w-11 place-items-center rounded-xl bg-[#dcefe4] text-[var(--brand)]"><UserRound size={20}/></div><div className="mt-6 grid gap-4 sm:grid-cols-2 [&_input]:mt-2 [&_input]:w-full [&_input]:rounded-xl [&_input]:border [&_input]:border-[var(--line)] [&_input]:px-3 [&_input]:py-3 [&_input]:outline-none [&_input]:focus:border-[var(--brand)]"><label className="block text-sm font-bold sm:col-span-2">Nama lengkap<input name="full_name" defaultValue={profile?.full_name || ""} placeholder="Nama kamu"/></label><label className="block text-sm font-bold">Universitas<input name="university" defaultValue={profile?.university || ""} placeholder="Nama universitas"/></label><label className="block text-sm font-bold">Program studi<input name="major" defaultValue={profile?.major || ""} placeholder="Program studi"/></label></div><button className="mt-6 rounded-xl bg-[var(--brand)] px-4 py-3 text-sm font-bold text-white hover:bg-[var(--brand-dark)]">Simpan perubahan</button></form></div>; }
